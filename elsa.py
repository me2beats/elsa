import random

def напечатать_привет():
    print("привет")

def напечатать_пока():
    print("пока")

def сообщение_системе(text):
    # Вызываем случайную функцию из списка функций
    if я['функции']:
        chosen_function = random.choice(я['функции'])
        chosen_function()

я = {
    'функции': [напечатать_привет, напечатать_пока],
    'контексты': [],
    'наблюдения': [],
    'гипотезы': [],
    'действия': [],
    'изменения': []
}

# Вызов функции при запуске программы
сообщение_системе("привет")